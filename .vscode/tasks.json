{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake-build-debug",
            "type": "shell",
            "command": "mkdir -p build && cd build && cmake -DCMAKE_BUILD_TYPE=Debug .. && make -j$(nproc)",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compiles the AIJVM project with debug symbols.",
            "problemMatcher": ["$gcc"]
        }
    ]
}