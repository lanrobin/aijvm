We are tackling a critical and complex phase in the AIJVM Execution Engine: Class Initialization (`<cinit>`) and Instance Initialization (`<init>`). 

Based on the JVM Specification (SE 26) Chapter 5.5 (Initialization), I need to design the mechanisms to handle these properly. Please note the architectural distinction: `<cinit>` requires implicit recursive handling by the JVM, while `<init>` is explicitly driven by the `invokespecial` bytecode generated by `javac`.

Please provide the C++20 header definitions and structural design for this logic.

Key technical requirements and design points:

1. **Class State Management (`<cinit>` tracking):** - We need to prevent `<cinit>` from running multiple times. Please propose an update to our `Class` (or `Klass`) structure to include an initialization state machine (e.g., `Uninitialized`, `Initializing`, `Initialized`). 
   - Consider how to handle thread-safety if multiple threads try to trigger `<cinit>` simultaneously (e.g., using `std::mutex` or `std::atomic` inside the class metadata).

2. **Implicit `<cinit>` Invocation Logic:**
   - The interpreter must intercept instructions like `new`, `getstatic`, `putstatic`, and `invokestatic`.
   - Before executing these, the JVM must check the class state. If `Uninitialized`, it MUST implicitly trigger the initialization process.
   - **Crucial Rule:** The JVM must recursively ensure the Parent class's `<cinit>` is executed BEFORE the Child class's `<cinit>`. Please propose an `initialize_class(Klass* target_class)` method (perhaps in the `ExecutionEngine` or `JavaThread` class) that handles this recursive frame pushing.

3. **Method Invocation and `<init>`:**
   - Since `javac` inherently inserts `invokespecial` for the parent's `<init>` inside the child's `<init>`, our interpreter does NOT need to magically look up parent constructors.
   - However, we DO need a robust way to invoke methods. Please propose a method (e.g., `invoke_method(Method* method, std::span<Slot> args)`) that creates a new `Frame`, populates its Local Variable Array with the arguments (including `this` reference), pushes it onto the `JavaThread` stack, and shifts the PC.

4. **Handling `new` Instruction:**
   - Propose the C++ definition for the `new` opcode logic. It should trigger class initialization (if needed), allocate memory in our `Heap` to create an `ObjectReference` (which holds a pointer to its `Class` and its instance fields), and push this reference onto the operand stack.

Following our `claude.md` rules, please provide ONLY the header definitions (`.hpp`) and brief architectural explanations first. Do not implement the full `.cpp` loops yet. I want to review the exact state machine and frame-pushing contract first.